{
  "name": "sovereign",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.1.38"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true,
  "scripts": {
    "test:programs": "bun run --cwd=./programs test",
    "cleanup": "bun run --cwd=./programs cleanup"
  },
  "type": "module",
  "workspaces": [
    "programs/*",
    "agents/*",
    "circuits/*",
    "backend/*",
    "validator/*"
  ]
}
