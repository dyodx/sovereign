{
  "name": "sovereign",
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.1.38",
    "turbo": "^2.3.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "private": true,
  "type": "module",
  "workspaces": [
    "programs/*",
    "agents/*",
    "circuits/*"
  ],
  "scripts": {
    "test:programs": "bun run --cwd=./programs test",
    "cleanup": "bun run --cwd=./programs cleanup"
  }
}
